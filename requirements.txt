# Core dependencies
Django==5.2.17  # Current LTS. 4.2 reached end of life at 4.2.30, so its last 6 CVEs had no fix in that series.
# nltk removed: nothing in this repo imports it (stopword lists live in stopwords/,
# tokenization is hand-rolled), and it has a steady drip of path-traversal CVEs --
# CVE-2026-81726 (model-artifact APIs bypass pathsec) affects every release through
# 3.10.3, which is still the latest on PyPI, so there is no fixed version to pin.
numpy>=2.0.0
scipy>=1.15.0
GitPython>=3.1.61
python-dateutil>=2.9.0  # Replaces DateTime

# For downloading State of the Union addresses
requests>=2.31.0
beautifulsoup4>=4.12.0

# Optional but recommended
django-debug-toolbar>=4.4.0
django-extensions>=4.1
gunicorn>=23.0.0
docopt>=0.6.2
chardet>=7.6.0  # 7.x removed chardet.universaldetector; basic_tools imports UniversalDetector from the package root

# For testing
pytest>=9.0.0
pytest-django>=4.14.0
pytest-cov>=7.1.0  # CI passes --cov; see TASKS.md 4.2. No threshold until 4.1 lands.
selenium>=4.30.0

# For LLM-based topic naming (optional)
openai>=1.0.0

# For BERTopic topic modeling (optional)
# NOTE: BERTopic dependencies (specifically numba) currently require Python <=3.13
# If using Python 3.14+, BERTopic installation will fail until dependencies are updated
bertopic>=0.16.0
sentence-transformers>=2.2.0
umap-learn>=0.5.12
hdbscan>=0.8.44
