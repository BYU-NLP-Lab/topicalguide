version: 2

updates:
  # Python. This was already being scanned via requirements.txt; declaring it
  # here turns on update pull requests as well as alerts.
  #
  # Grouped into ONE pull request. Most of requirements.txt is declared with
  # ">=", so Dependabot proposes raising every floor to the newest release and
  # opens a separate PR per package -- fifteen of them on the first sweep, each
  # editing requirements.txt and so each conflicting with whichever merged
  # first. Grouping keeps the updates without the conflict storm.
  #
  # Note these floor bumps do not change what actually gets installed: ">="
  # already resolves to the newest compatible release. They only raise the
  # minimum, so treat them as low-risk housekeeping and read the changelog for
  # anything crossing a major version.
  - package-ecosystem: pip
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    groups:
      python-dependencies:
        patterns:
          - "*"
    ignore:
      # This project tracks Django LTS releases deliberately: 6.1's support
      # ends four months EARLIER than the 5.2 LTS already installed, and 6.x
      # additionally requires Python 3.12+. See TASKS.md 2.5. Without this,
      # grouping quietly swept "Django 5.2.17 -> 6.1" in beside a dozen
      # harmless floor bumps, where merging the group would have reversed that
      # decision without anyone reading the diff.
      #
      # Security updates are unaffected: they are configured separately from
      # version updates and still arrive.
      - dependency-name: django
        update-types:
          - version-update:semver-major

  # Front-end libraries. These are VENDORED, not installed -- /package.json
  # exists only so this ecosystem has a manifest for Dependabot to read.
  #
  # open-pull-requests-limit: 0 disables PULL REQUESTS while leaving SECURITY
  # ALERTS on, which is the whole point of the manifest. Automated PRs here are
  # actively dangerous: they edit one line of JSON and do not touch the
  # committed asset under visualize/static/, so merging one would mark a CVE
  # resolved while the vulnerable file is still served. Dependabot's first batch
  # proposed exactly that, including lodash 2.4.1 -> 4.18.1 against a critical
  # advisory.
  #
  # Upgrade by hand instead: replace the file, update the <script> tag and the
  # table in visualize/static/VENDOR.md, run the browser tests, then bump the
  # version here in the same commit.
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 0

  # Workflow actions. CI currently warns that actions/checkout@v4 and
  # actions/setup-python@v5 target the deprecated Node.js 20.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
